# Security

Please report security issues privately to the repository owner. Do not open a
public issue with exploit details.

Media files, URLs, OAuth responses, external-tool output, and `.keywave`
packages are untrusted input. Creator must enforce its resource limits, never
invoke a shell with user-controlled text, never import browser credentials, and
never bypass authentication, DRM, regional restrictions, signatures, or access
controls.
